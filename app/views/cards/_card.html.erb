<div id="<%= dom_id card %>" class="card">
  <details>
    <summary>
      <span><%= card.title %> <%= card.body.blank? ? "" : "📄" %><%= link_to heroicon("trash", variant: :outline, options: { style: "color: red; width: 1em; height: 1em;" }), card, method: :delete %></span>
    <%= card.body %>
  </details>
</div>
