<div id="<%= dom_id card %>" class="card" data-id="<%= card.id %>" data-url="/cards/:id" data-resource="card">
  <h3 class="card-title"><%= card.title %> <%= card.body.blank? ? "" : "📄" %><%= button_to heroicon("trash", variant: :outline, options: { style: "color: red; width: 1em; height: 1em;" }), card, method: :delete %></h3>
  <p class="card-body"><%= sanitize(card.body.to_s.truncate_words(10)) %></p>
</div>
